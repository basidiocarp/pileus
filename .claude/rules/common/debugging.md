# Debugging

Fix root cause, not just the symptom.

## Rules

- reproduce the problem before changing code
- read the full error, trace, and recent changes before patching
- if three fixes at the same level fail, escalate from implementation to design or requirements
- escalate early when each fix reveals a new failure elsewhere or the issue is obviously architectural
- use the `systematic-debugging` skill for the full workflow
